// Public API for the newsletter signup feature.
//
// Consumers import from `@/features/newsletter` and never reach into the
// individual files below.

export { NewsletterForm } from "./NewsletterForm";
export type { NewsletterFormProps } from "./NewsletterForm";

export { NewsletterModal } from "./NewsletterModal";
export type { NewsletterModalProps } from "./NewsletterModal";
