// Public API for the contact feature.
//
// Consumers import from `@/features/contact` and never reach into the
// individual files below.

export { ContactForm } from "./ContactForm";
export type { ContactFormProps } from "./ContactForm";
